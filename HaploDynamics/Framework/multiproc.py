import multiprocessing as mp

class MPgenmatrix(object):

  def __init__(self):
    print("The class \"MPgenmatrix\" is still under construction")


#def F(exome,amino_acids,name_ensg,debug_message):
  ##make a change in memory
  #pass


#if __name__ == '__main__':

  #exome       = content_to_store(load_content("general/exons.ensg-list.txt.gz"))
  #amino_acids = load_content("0-amino_acid.txt")
  #name_ensg = load_content("general/name.ensg.txt")

  #make_directory(os.getcwd(),"curated_cds")

  #def parallel_F(u):
   #return F(exome[u],amino_acids,name_ensg,u)

  #pool = mp.Pool(mp.cpu_count()-1)
  #pool.map(parallel_F, range(len(exome)))
